# =====================================================
# 🍕 DomiFlash - Configuración para PostgreSQL (Render)
# =====================================================

# 🗄️ Configuración de Base de Datos PostgreSQL
# En Render, estos valores vienen de tu base de datos PostgreSQL
DB_HOST=dpg-xxxxx-a.oregon-postgres.render.com
DB_USER=domiflash_user
DB_PASSWORD=tu_password_de_render
DB_NAME=dbflash

# 🔐 Clave secreta de Flask (genera una nueva para producción)
# Puedes generar una con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_clave_super_secreta_cambiar_en_produccion

# 📧 Configuración de Email (Gmail)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_app_password_gmail
MAIL_DEFAULT_SENDER=tu_email@gmail.com

# 🕐 Configuración de Sesiones
SESSION_TIMEOUT_MINUTES=30
SESSION_WARNING_MINUTES=5
SESSION_COOKIE_SECURE=True

# 🚫 Límites de Seguridad
MAX_LOGIN_ATTEMPTS=5
LOGIN_ATTEMPT_TIMEOUT_MINUTES=15

# 🌐 Configuración de Producción
FLASK_ENV=production
FLASK_DEBUG=False
